# Lab 5 — Tool Calling Agent on top of Lab 1-4
#
# No new pip packages are required for Lab 5 itself: tool calling and
# image input are handled via the Ollama HTTP API using `requests`,
# and `zoneinfo` (stdlib) powers the datetime tool.

# Lab 1 + Lab 2 + Lab 3 dependencies
pyTelegramBotAPI==4.32.0
scikit-learn>=1.5.0
matplotlib>=3.8.0
seaborn>=0.13.0
wordcloud>=1.9.0
python-dotenv>=1.0.0
numpy>=1.26.0
pandas>=2.0.0
nltk>=3.8.0
spacy>=3.7.0
tensorflow>=2.16.0
transformers>=4.30.0
textblob>=0.18.0
stanza>=1.8.0
datasets>=3.0.0
gensim>=4.3.0
scipy>=1.13.0

# Lab 4 additions
torch>=2.0.0
sentencepiece>=0.2.0
sacremoses>=0.1.0
langdetect>=1.0.9
requests>=2.31.0
networkx>=3.2

# After install:
#   python -m spacy download pl_core_news_sm
#   python -m spacy download en_core_web_sm
#   python -m textblob.download_corpora
# Local LLM (Lab 4 summarization + Lab 5 agent):
#   ollama serve
#   ollama pull qwen2.5:1.5b   (controller / native function calling)
#   ollama pull qwen2.5vl:3b   (Lab 5 vision tool — ~3.2 GB)
